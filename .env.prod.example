# Production environment.

OCR_ENGINE=paddleocr_vl
OCR_DEVICE=gpu:0

MODEL_CACHE_DIR=.paddlex_cache

# Do not set OCR_COMPRESS_MAX_SIDE in production unless explicitly needed.

MAX_IMAGE_BYTES=10485760
LOG_LEVEL=INFO
